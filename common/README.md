# Common

This package contains code that is shared among two or more services. The code is placed in multiple directories under src/. Each file that shall be possible to import must (unfortunately) be listed under `exports` in `package.json`.
